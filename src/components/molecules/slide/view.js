import React from "react";

import { ContainerSlide, TextWrapper, TextSlide, Illustration } from "./styled";

const Slide = ({ alt, src, tabIndex }) => {
  return <ContainerSlide></ContainerSlide>;
};

export default Slide;
